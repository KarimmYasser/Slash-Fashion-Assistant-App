import 'package:flutter/material.dart';

class ApproveBrandsScreen extends StatelessWidget {
  const ApproveBrandsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('brands'),
    );
  }
}
