import 'package:flutter/material.dart';

class DeleteReviewsScreen extends StatelessWidget {
  const DeleteReviewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('reviews'),
    );
  }
}
